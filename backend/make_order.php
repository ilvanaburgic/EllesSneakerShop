<?php

//require_once __DIR__ . '/rest/services/OrderService.class.php';

//header('Content-Type: application/json');

// Instantiate the OrderService
//$orderService = new OrderService();

// Get the JSON POST body
//$json = file_get_contents('php://input');
//$data = json_decode($json, true);

// Validate the input
//if (!isset($data['user']) || !isset($data['items'])) {
  //http_response_code(400);
  //echo json_encode(['error' => 'Missing user or items data']);
  //exit;
//}

// Use the OrderService to create the order with the user
//$order = $orderService->createOrderWithUser($data);
//echo json_encode(['success' => true, 'message' => 'Order created successfully', 'order' => $order]);
