<?php

//require_once __DIR__ . '/rest/services/NewsletterService.class.php';

// Decode JSON from the request body
//$input_data = json_decode(file_get_contents('php://input'), true);

//$newsletter_service = new NewsletterService();

// Here, you would get the subscriber data from the $input_data and call the subscribe method
//$subscriber = [
  //'email' => $input_data['email']
//];

// Add additional checks if needed, like validating email, checking if already subscribed, etc.

//$newsletter_service->subscribe($subscriber);

//header('Content-Type: application/json');
//echo json_encode([
  //'message' => 'Subscription successful.'
//]);
